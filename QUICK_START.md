# 🚀 배치 자동 학습 빠른 시작 가이드

## 📌 한눈에 보기

```bash
# 1. 터미널 1: 개발 서버 실행
npm run dev

# 2. 터미널 2: 데이터 검증 (선택)
npm run validate-data

# 3. 터미널 2: 배치 학습 시작
npm run batch-learn
```

---

## 🎯 3단계 완벽 가이드

### 1️⃣ 개발 서버 시작

```bash
npm run dev
```

✅ 확인:
- `http://localhost:5173` 또는 `http://localhost:5175` 에서 실행 확인
- 웹 브라우저에서 "한국 수화 번역기" 페이지 열림

### 2️⃣ 데이터 검증 (옵션)

```bash
# 데이터가 제대로 매칭되는지 확인
npm run validate-data
```

예상 출력:
```
✅ 발견된 영상: 1500개
✅ 발견된 JSON: 3000개
✅ 완벽한 매칭: 1500개
🎯 성공률: 100.0%
```

### 3️⃣ 배치 학습 시작

```bash
# 모든 파일 자동 처리
npm run batch-learn
```

또는 특정 파일만:
```bash
# 1510번부터 시작
npm run batch-learn -- --start 1510

# 처음 50개만 처리
npm run batch-learn -- --count 50

# 1510번부터 50개
npm run batch-learn -- --start 1510 --count 50
```

---

## 📊 실행 화면 예시

### 터미널 1: npm run dev
```
  VITE v6.4.1  ready in 234 ms

  ➜  Local:   http://localhost:5173/
  ➜  press h to show help
```

### 터미널 2: npm run validate-data
```
✅ 발견된 영상: 1500개
✅ 발견된 JSON: 3000개
✅ 완벽한 매칭: 1500개
🎯 성공률: 100.0%
```

### 브라우저: 배치 학습 진행

```
[1/1500] 📁 처리 중: 1501 ("운전면허")
   🌐 페이지 로드 중...
   🔘 "수화 학습하기" 버튼 클릭...
   📤 파일 업로드 중...
   ▶️  비디오 처리 시작...
   ⏳ 비디오 처리 대기 중...
   ✅ 비디오 처리 완료!
   ✍️  수화 이름 입력: "운전면허"
   💾 저장 중...
   ✅ "운전면허" 저장 완료!
```

---

## 💡 팁

### 🚨 트러블슈팅

**Q: "개발 서버에 연결할 수 없습니다"**
- 터미널 1에서 `npm run dev` 실행 확인
- 포트 5173/5175 가 사용 중인지 확인

**Q: 배치 학습이 멈춤**
- 스크립트 로그 확인
- 웹사이트 응답 속도 확인
- `Ctrl+C` 로 중단하고 `--start` 옵션으로 재시작

**Q: 일부 파일이 스킵됨**
```bash
# 상세 검증
npm run validate-data -- --verbose
```

### 📈 대량 처리 팁

**예시: 1500개 파일을 150개씩 10번으로 분할**

```bash
# 배치 1 (1501-1650)
npm run batch-learn -- --start 1501 --count 150

# 배치 2 (1651-1800)
npm run batch-learn -- --start 1651 --count 150

# ... 계속 ...

# 배치 10 (2851-3000)
npm run batch-learn -- --start 2851 --count 150
```

### ⚡ 성능 최적화

**빠른 처리:**
- 배치로 나누어 처리 (메모리 효율화)
- 네트워크가 좋은 환경에서 실행
- 영상 처리 시간에 따라 타임아웃 조정

---

## 🔗 더 알아보기

자세한 옵션과 설정을 보려면:
```bash
cat BATCH_LEARNING_GUIDE.md
```

---

## 📋 체크리스트

- [ ] Node.js 설치됨
- [ ] `npm install` 완료됨
- [ ] 개발 서버 실행 (`npm run dev`)
- [ ] 02 폴더에 영상 파일 있음
- [ ] 17 폴더에 JSON 메타데이터 있음
- [ ] 검증 통과 (`npm run validate-data`)
- [ ] 배치 학습 시작 (`npm run batch-learn`)

---

## ⏱️ 예상 처리 시간

| 파일 개수 | 예상 시간 |
|----------|----------|
| 10개 | 10-15분 |
| 50개 | 50-80분 |
| 100개 | 100-160분 |
| 500개 | 8-12시간 |

---

**시작하세요!** 🎉
