# 🤖 배치 자동 수화 학습 가이드

## 📌 개요

`batch-learn.mjs` 스크립트는 **02 폴더의 영상 파일**과 **17 폴더의 JSON 메타데이터**를 자동으로 매칭하여 Puppeteer를 이용해 브라우저에서 자동으로 수화를 학습시킵니다.

### 매칭 방식
- **영상 파일**: `02/1501.mp4`, `02/1502.mp4`, ...
- **메타데이터**: `17/1501.json`, `17/1502.json`, ...
- **수화 이름**: JSON 파일의 `data[0].attributes[0].name` 필드

예시:
```json
{
  "data": [
    {
      "attributes": [
        {
          "name": "운전면허"  // ← 이 값이 수화 이름
        }
      ]
    }
  ]
}
```

---

## 🚀 빠른 시작

### 1️⃣ 개발 서버 실행

먼저 Vite 개발 서버를 시작합니다:

```bash
npm run dev
```

서버가 `http://localhost:5173` 또는 `http://localhost:5175`에서 실행됨을 확인하세요.

### 2️⃣ 배치 학습 실행

새로운 터미널에서:

```bash
# 모든 파일 자동 처리
npm run batch-learn

# 또는 특정 ID부터 시작 (예: 1510)
npm run batch-learn -- --start 1510

# 또는 처음 5개만 처리
npm run batch-learn -- --count 5

# 조합 사용 (1510부터 시작해서 10개만)
npm run batch-learn -- --start 1510 --count 10
```

---

## 📋 사용 옵션

| 옵션 | 설명 | 예시 |
|------|------|------|
| `--start <ID>` | 특정 ID부터 시작 | `npm run batch-learn -- --start 1505` |
| `--count <N>` | 최대 N개 파일만 처리 | `npm run batch-learn -- --count 20` |
| 조합 | 둘 다 사용 가능 | `npm run batch-learn -- --start 1510 --count 10` |

---

## 📊 실행 흐름

```
배치 학습 시작
    ↓
02 폴더에서 모든 .mp4 파일 검색
    ↓
각 파일에 대해:
  1. 대응하는 JSON 파일 확인 (17폴더)
  2. JSON에서 수화 이름 추출
  ↓
브라우저 자동화 (Puppeteer) 시작
    ↓
각 파일별로:
  1. 웹사이트 접속
  2. "수화 학습하기" 버튼 클릭
  3. "영상 업로드" 탭 이동
  4. 영상 파일 업로드
  5. "비디오 처리 시작" 클릭
  6. 처리 완료 대기 (최대 120초)
  7. 수화 이름 입력
  8. 저장 버튼 클릭
  9. 저장 완료 확인
  ↓
모든 파일 처리 완료
  └─ 성공/실패 요약 출력
```

---

## 🎯 스크립트 동작 방식

### 1. 파일 검색 및 검증
- 02 폴더에서 모든 .mp4 파일 찾기
- ID 순서로 정렬 (1501, 1502, ... → 숫자 순)
- 각 파일마다 대응하는 JSON 메타데이터 확인

### 2. 자동화 처리
- Puppeteer로 브라우저 제어
- 각 파일마다 **새로운 탭/페이지** 사용
- 각 단계별 대기 시간으로 안정성 확보

### 3. 에러 핸들링
- JSON 파일 없음 → 스킵
- 영상 파일 없음 → 스킵
- 버튼/입력 필드 못 찾음 → 다음 파일로 진행
- 처리 완료 메시지 못 찾음 → 일단 계속 진행

### 4. 최종 리포트
```
═══════════════════════════════════════════════════
📊 처리 완료 요약
═══════════════════════════════════════════════════
✅ 성공: 18개
❌ 실패: 2개

실패한 파일 ID:
  - 1515
  - 1520

재시도 커맨드:
  npm run batch-learn -- --start 1515
```

---

## ⚙️ 커스터마이징

### 처리 속도 조정

`batch-learn.mjs`에서 다음 값을 수정할 수 있습니다:

```javascript
// 각 단계 사이의 대기 시간 (밀리초)
await delay(1000);  // 1초

// 배치 간 대기 시간 (밀리초)
const waitTime = 2000;  // 2초
```

### 타임아웃 조정

```javascript
// 비디오 처리 최대 대기 시간 (초)
for (let i = 0; i < 120; i++) {  // 120초 = 2분
  // ...
}
```

### 브라우저 옵션

```javascript
const browser = await puppeteer.launch({
  headless: false,  // false: UI 표시, true: 백그라운드
  defaultViewport: { width: 1400, height: 900 }  // 해상도
});
```

---

## 🔍 디버깅 팁

### 1. 특정 파일만 테스트하기

```bash
# 1505번부터 1510번까지만 5개 처리
npm run batch-learn -- --start 1505 --count 5
```

### 2. 브라우저 UI 확인

스크립트는 `headless: false`로 설정되어 있으므로 브라우저가 표시됩니다.
- 각 단계를 실시간으로 확인 가능
- 문제 발생 시 직접 확인 가능

### 3. 콘솔 로그 확인

스크립트는 매 단계마다 진행 상황을 출력합니다:

```
[1/50] 📁 처리 중: 1501 ("운전면허")
   🌐 페이지 로드 중...
   🔘 "수화 학습하기" 버튼 클릭...
   📤 파일 업로드 중...
   ▶️  비디오 처리 시작...
   ⏳ 비디오 처리 대기 중... (최대 120초)
   ✅ 비디오 처리 완료!
   ✍️  수화 이름 입력: "운전면허"
   💾 저장 중...
   ✅ "운전면허" 저장 완료!
```

---

## ❌ 문제 해결

### Q: "개발 서버에 연결할 수 없습니다"

**A:** 다른 터미널에서 `npm run dev`를 실행했는지 확인하세요.

```bash
# 터미널 1
npm run dev

# 터미널 2 (새로 열기)
npm run batch-learn
```

### Q: 일부 파일이 스킵됩니다

**A:** 스킵되는 이유:
1. **JSON 파일 없음** → `17/파일ID.json` 확인
2. **영상 파일 없음** → `02/파일ID.mp4` 확인
3. **JSON에 이름 없음** → `data[0].attributes[0].name` 필드 확인

### Q: 처리 중 멈춤

**A:** 다음을 확인하세요:
1. 웹사이트 응답 속도 확인
2. 타임아웃 값 증가: `for (let i = 0; i < 180; i++)` (3분)
3. 대기 시간 증가: `await delay(3000)` (3초)

### Q: 브라우저 자동 종료

**A:** 스크립트가 모든 파일 처리를 완료하면 자동으로 브라우저가 닫힙니다.
- 강제 종료: `Ctrl+C` (터미널)

---

## 📈 대량 처리 예시

### 100개 파일을 10개씩 배치로 처리

```bash
# 첫 10개
npm run batch-learn -- --count 10

# 다음 10개 (11번부터 20번)
npm run batch-learn -- --start 1511 --count 10

# ... 계속 ...

# 마지막 10개
npm run batch-learn -- --start 1591 --count 10
```

### 실패한 파일만 재처리

```bash
# 1510부터 시작
npm run batch-learn -- --start 1510

# 처리 완료 후, 실패 목록에서 첫 번째 ID부터 다시 시작
npm run batch-learn -- --start 1515
```

---

## 📊 성능 예상

| 파일 개수 | 예상 시간 | 비고 |
|----------|----------|------|
| 5개 | 5~10분 | 배운 후 대기 포함 |
| 10개 | 10~20분 | - |
| 50개 | 50~100분 | 네트워크 상태에 따라 변동 |
| 100개+ | 2~3시간 | 야간 자동 처리 권장 |

---

## 🎯 상세 알고리즘

### 파일 선택 알고리즘

```
1. getVideoFileIds()
   └─ 02 폴더의 모든 .mp4 파일 검색
   └─ ID만 추출 (파일명에서 .mp4 제거)
   └─ 숫자 순서로 정렬

2. getSignNameFromJson(fileId)
   └─ 17폴더에서 파일ID.json 찾기
   └─ JSON 파싱: data[0].attributes[0].name 추출
   └─ 없으면 null 반환

3. videoExists(fileId)
   └─ 02 폴더에서 파일ID.mp4 존재 확인
```

### 자동화 알고리즘

```
learnSignWithPuppeteer(browser, fileId, signName)
  ↓
1. 새로운 브라우저 페이지 생성
   
2. 페이지 로드
   └─ http://localhost:5173 접속
   └─ 네트워크 유휴 대기 (networkidle0)
   
3. 버튼/필드 찾기
   └─ 모든 button 요소 검색
   └─ textContent로 텍스트 비교
   └─ 정확한 매칭 필요
   
4. 자동화 액션
   └─ 클릭, 파일 업로드, 텍스트 입력
   └─ 각 단계 후 대기
   
5. 상태 확인
   └─ alert 요소 텍스트 검사
   └─ 성공/실패 메시지 확인
   
6. 페이지 종료
   └─ page.close()
```

---

## 🔗 관련 파일

- **스크립트**: [`scripts/batch-learn.mjs`](../scripts/batch-learn.mjs)
- **기본 설정**: [`package.json`](../package.json)
- **영상 데이터**: [`02/`](../02/)
- **메타데이터**: [`17/`](../17/)
- **학습 모드**: [LearningMode.tsx](../src/components/sign-language/LearningMode.tsx)
- **비디오 처리**: [VideoProcessor.tsx](../src/components/sign-language/VideoProcessor.tsx)

---

## 📝 로그 예시

```
═══════════════════════════════════════════════════
🚀 배치 자동 수화 학습 시작
═══════════════════════════════════════════════════

✅ 발견된 영상 파일: 50개

📍 시작 ID: 1501 (50개 파일)
📍 처리 개수 제한: 없음

📊 처리할 파일: 50개

🌐 브라우저 시작...

[1/50] 📁 처리 중: 1501 ("운전면허")
   🌐 페이지 로드 중...
   🔘 "수화 학습하기" 버튼 클릭...
   🔘 "영상 업로드" 탭 클릭...
   📤 파일 업로드 중...
   ▶️  비디오 처리 시작...
   ⏳ 비디오 처리 대기 중... (최대 120초)
   ⏳ 10초 경과...
   ⏳ 20초 경과...
   ✅ 비디오 처리 완료!
   ✍️  수화 이름 입력: "운전면허"
   💾 저장 중...
   ✅ "운전면허" 저장 완료!

⏳ 다음 파일 처리를 위해 2초 대기...

[2/50] 📁 처리 중: 1502 ("골키퍼")
   ...

═══════════════════════════════════════════════════
📊 처리 완료 요약
═══════════════════════════════════════════════════
✅ 성공: 48개
❌ 실패: 2개

실패한 파일 ID:
  - 1520
  - 1535

재시도 커맨드:
  npm run batch-learn -- --start 1520

✨ 배치 처리 완료!
```

---

## 🚨 주의사항

1. **개발 서버 필수**: `npm run dev`를 먼저 실행해야 합니다.
2. **네트워크 안정성**: 안정적인 인터넷 연결이 필요합니다.
3. **시간 소요**: 대량 처리 시 상당한 시간이 필요합니다 (파일당 1~3분).
4. **컴퓨터 부하**: Puppeteer와 브라우저가 높은 CPU/메모리를 사용합니다.
5. **자동화 감지**: 일부 보안 솔루션이 자동화를 감지할 수 있습니다.

---

## 💡 팁

- **야간 처리**: 많은 파일을 처리할 때는 야간에 실행하세요.
- **모니터링**: 첫 5-10개 파일 처리 후 상태를 확인하세요.
- **진행도 저장**: 성공/실패 목록을 별도 파일에 기록할 수 있습니다.
- **부분 처리**: `--start`와 `--count` 옵션으로 배치로 나누어 처리하세요.

---

생성일: 2025-12-21 | 버전: 1.0
